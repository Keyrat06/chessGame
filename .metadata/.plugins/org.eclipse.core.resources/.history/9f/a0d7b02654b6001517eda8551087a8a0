package tictactoe;

import static org.junit.Assert.*;

import org.junit.Test;

public class boardTest {

	@Test
	public void test() {
		Board newGame = new Board();
		
		newGame.move(1,1);
		newGame.move(0,1);
		newGame.move(1, 2);
		newGame.move(1, 0);
		newGame.move(2, 2);
		newGame.move(0, 0);
		
	}

}
